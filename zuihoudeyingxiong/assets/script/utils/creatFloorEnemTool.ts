import { _decorator, Component } from 'cc';

const { ccclass } = _decorator;

@ccclass('creatFloorEnemTool')
export class creatFloorEnemTool extends Component {
    start() {
        // enemy_3 uses Spine and is intentionally disabled.
    }
}
